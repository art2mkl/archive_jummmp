{% extends 'base.html.twig' %}

{% block title %}Edit Cv{% endblock %}

{% block body %}

    <main class="edit-form">

        <div class="edit-form-head">
            <h1><i class="fas fa-pencil-alt"></i>&nbsp;En-tête</h1>
            <a class="btn" href="{{ path('cv_show', {'id': app.session.get('id')}) }}"><i class="fas fa-undo-alt"></i></a>
        </div>

    {{ include('cv/_form.html.twig', {'button_label': 'Update'}) }}

    </main>

{% endblock %}
